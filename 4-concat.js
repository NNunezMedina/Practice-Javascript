#!/usr/bin/node

function concat(first, second) {
    if(first === undefined && second ===undefined) {
        console.log(`${first} is ${second}`);
    } else {
        first = first;
        second = second;
        console.log(`${first} is ${second}`);
    }
}

concat();
